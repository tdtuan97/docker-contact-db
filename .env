# common configure
TZ=Asia/ho_chi_minh

# server configs
SERVER_PORT=7001
WS_PORT=7002
WS_PATH=/ws-api
PREFIX=/api/v1

# swagger
SWAGGER_PATH=swagger-api
SWAGGER_ENABLE=true
SWAGGER_VERSION=1.0
SWAGGER_TITLE='System API documentation'
SWAGGER_DESC='NestJs CRUD for RESTful API: nestjs + mysql + typeorm + redis + jwt + swagger'